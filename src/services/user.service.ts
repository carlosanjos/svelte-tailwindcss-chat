import type { ProfileInterface } from "~/interfaces/profile.interface";

export class UserService {
    public getCurrentUser() {
    }

    public sendMessage(recipient: ProfileInterface, message: string) {
    }
}